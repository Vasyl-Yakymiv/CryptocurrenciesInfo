<Window x:Class="CryptocurrenciesInfo.Views.ConvertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CryptocurrenciesInfo.Views"
        mc:Ignorable="d"
        Title="{DynamicResource Convert_Window}" Height="350" Width="400" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" FontFamily="Segoe UI" FontSize="12" >
    <Grid Margin="20">
        <StackPanel Margin="-10,-12,-10,-18">
            <TextBlock Text="{DynamicResource Convertor_From}"/>
            <ComboBox x:Name="CurrencyFrom" DisplayMemberPath="Title" SelectedValuePath="Id" />

            <TextBlock Text="{DynamicResource Convertor_To}" Margin="0,10,0,0"/>
            <ComboBox x:Name="CurrencyTo" DisplayMemberPath="Title" SelectedValuePath="Id" />

            <TextBlock Text="{DynamicResource Convertor_Amount}" Margin="0,10,0,0"/>
            <TextBox x:Name="Amount"/>

            <Button Content="{DynamicResource Convert_Button}" Click="Convert_Button_Click" Margin="0,10,0,0" Width="350" Height="20"/>
            <TextBlock x:Name="Result" FontWeight="Bold" FontSize="16" Margin="0,10,0,0"/>
            <Button Content="{DynamicResource Back_Button}" Width="350" Click="Back_Button_Click" Height="20"/>
        </StackPanel>
    </Grid>
</Window>
