<Window x:Class="CryptocurrenciesInfo.Views.ConvertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CryptocurrenciesInfo.Views"
        mc:Ignorable="d"
        Title="ConvertWindow" Height="450" Width="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="From:"/>
            <ComboBox x:Name="CurrencyFrom" DisplayMemberPath="Title" SelectedValuePath="Id" />
            <TextBlock Text="To:" Margin="0,10,0,0"/>
            <ComboBox x:Name="CurrencyTo" DisplayMemberPath="Title" SelectedValuePath="Id" />
            <TextBlock Text="Amount:" Margin="0,10,0,0"/>
            <TextBox x:Name="Amount"/>
            <Button Content="Convert" Click="Convert_Button_Click" Margin="0,10,0,0"/>
            <TextBlock x:Name="Result" FontWeight="Bold" FontSize="16" Margin="0,10,0,0"/>
            <Button Content="Back" Width="762" Click="Back_Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
