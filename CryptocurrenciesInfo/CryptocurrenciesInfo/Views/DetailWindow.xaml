<Window x:Class="CryptocurrenciesInfo.Views.DetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CryptocurrenciesInfo.Views"
        mc:Ignorable="d"
        Title="DetailWindow" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Currency.Title}" 
                       FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding Currency.Symbol}" FontSize="16" Margin="0,0,0,5"/>
            <TextBlock Text="{DynamicResource Price}" FontWeight="Bold"/>
            <TextBlock Text="{Binding Currency.Price}" Margin="0,0,0,10"/>

            <TextBlock Text="{DynamicResource 24h_Volume}" FontWeight="Bold"/>
            <TextBlock Text="{Binding Currency.Volume24h}" Margin="0,0,0,10"/>

            <TextBlock Text="{DynamicResource 24h_Change}" FontWeight="Bold"/>
            <TextBlock Text="{Binding Currency.PriceChange24h}" Margin="0,0,0,10"/>
        </StackPanel>
    </Grid>
</Window>
